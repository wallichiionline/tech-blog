<div class="container">
    <div class="row">
        <div class="col">
            {{#if blogPostDisplay.length}}
                {{#each blogPostDisplay as |bp|}}
                    {{>blogPost blogPost=bp}}
                {{/each}}
            {{else}}
            <h3> Go grab some coffe or tea :) </h3>
            {{/if}}
        </div>
    </div>
</div>

